zh-TW:
  # model and attributes name
  activerecord: &default
    models:
      product/image:
        one: 商品圖片
        other: 商品圖片

    attributes:
      product/image:
        image: 圖片
        use_case: 用於

    errors:
      models:
        product/image:
          attributes:
  activemodel: *default

  # Enums
  enums:
    product/image:
      use_case:
        normal: 商品
        description: 敘述插圖

  # AASM state machine
  aasm_state:
    product/image:
