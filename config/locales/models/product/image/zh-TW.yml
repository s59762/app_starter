zh-TW:
  # model and attributes name
  activerecord: &default
    models:
      product/image:
        one: 商品圖片
        other: 商品圖片

    attributes:
      product/image:
        image: 圖片
        variant_id: 所屬規格
        general_images: 不屬於任何規格
        images_for: '屬於 %{variant_name} 的圖片'

    errors:
      models:
        product/image:
          attributes:
  activemodel: *default

  # Enums
  enums:
    product/image:

  # AASM state machine
  aasm_state:
    product/image:
